输入输入重定向：
    date >> 1.txt 2>&1         date >> /etc/null 2>&1
date >> 1.txt 2>>2.txt


多命令执行：
;
&&
| |

数值运算：
a=1
b=2
c=$(($a+$b))
echo $c


申明变量类型:
declare 
- 给定类型
+ 取消类型
-i 设为整数
-x 设为环境变量
-p 查看变量类型
例：
aa=1 
bb=2
declare -i cc=$aa+$bb
echo $cc

expr,let数值运算工具
a=1
b=2
cc=$(expr $a + $b)
